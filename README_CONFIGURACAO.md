# RifaPop — configuração

Chave Pix configurada:
73981602717

O WhatsApp ainda não foi configurado porque o número não foi informado.

ATENÇÃO:
- A versão original usa armazenamento local (`localStorage`) para reservas.
- Isso não sincroniza reservas entre celulares.
- Antes de divulgar a rifa, recomenda-se implementar um banco de dados/backend para evitar reservas duplicadas.
